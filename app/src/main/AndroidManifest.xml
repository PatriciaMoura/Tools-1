<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="course.example.tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Calculadora" />
        <activity android:name=".IMC" />
        <activity android:name=".SpeechToText" />
        <activity android:name=".Calcular_Media" />
        <activity
            android:name=".Ficha_Medica_Controller"
            android:label="@string/title_activity_ficha__medica"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Livro_Controller" />
        <activity android:name=".consultaLayout" />
        <activity android:name=".Games"></activity>
    </application>

</manifest>